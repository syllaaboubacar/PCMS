<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
            http://maven.apache.org/POM/4.0.0
            https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <!-- ============================================================
         Configuration générale Maven
         ============================================================ -->

    <modelVersion>4.0.0</modelVersion>

    <!-- ============================================================
         Parent Spring Boot
         ============================================================ -->

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <!-- ============================================================
         Informations du projet
         ============================================================ -->

    <groupId>lu.police</groupId>
    <artifactId>pcms-backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>

    <name>PCMS Backend</name>

    <description>
        Police Case Management System Backend
    </description>

    <url>
        https://github.com/syllaaboubacar/PCMS
    </url>

    <!-- ============================================================
         Propriétés du projet
         ============================================================ -->

    <properties>

        <!-- Version Java utilisée par le projet -->
        <java.version>21</java.version>

        <!--
            Version stable de MapStruct.

            1.6.3 est actuellement la dernière version stable.
            Nous évitons volontairement les versions Beta
            pour le projet PCMS.
        -->
        <mapstruct.version>1.6.3</mapstruct.version>

        <!--
            Binding permettant à MapStruct de fonctionner
            correctement avec les annotations générées par Lombok.
        -->
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>

    </properties>


    <!-- ============================================================
         Dépendances
         ============================================================ -->

    <dependencies>

        <!-- ========================================================
             REST API
             ======================================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>


        <!-- ========================================================
             Validation Jakarta Bean Validation
             ======================================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>


        <!-- ========================================================
             Spring Data JPA
             ======================================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>


        <!-- ========================================================
             PostgreSQL
             ======================================================== -->

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>


        <!-- ========================================================
             Flyway
             ======================================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>


        <!-- ========================================================
             Lombok
             ======================================================== -->

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>


        <!-- ========================================================
             MapStruct
             ========================================================

             Cette dépendance fournit les annotations MapStruct
             utilisées dans les interfaces de mapping.

             Exemple :

             @Mapper(componentModel = "spring")
             public interface RoleMapper {
                 ...
             }

             ======================================================== -->

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>


        <!-- ========================================================
             Tests
             ======================================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>


        <!-- ========================================================
             Tests Spring Data JPA
             ======================================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>


        <!-- ========================================================
            Tests des contrôleurs Web (Spring Boot 4)
            ========================================================
            Contient : @WebMvcTest, MockMvc, @MockBean,
            et les utilitaires de test pour les contrôleurs REST.
            ======================================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>


        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>3.0.3</version>
        </dependency>
        

    </dependencies>


    <!-- ============================================================
         Build
         ============================================================ -->

    <build>

        <plugins>

            <!-- ====================================================
                 Spring Boot Maven Plugin
                 ====================================================

                 Permet notamment de construire l'application
                 Spring Boot exécutable.

                 ==================================================== -->

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.build.directory}/generated-sources/annotations</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>


            <!-- ====================================================
                 Maven Compiler Plugin
                 ====================================================

                 Configure :
                 - Java 21
                 - Lombok
                 - MapStruct
                 - Lombok/MapStruct binding
                 - les annotation processors

                 ==================================================== -->

            <plugin>

                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>

                <version>3.15.0</version>

                <configuration>

                    <!-- Version du langage Java -->
                    <source>${java.version}</source>

                    <!-- Version de bytecode générée -->
                    <target>${java.version}</target>


                    <!-- =================================================
                         Annotation Processors
                         ================================================= -->

                    <annotationProcessorPaths>

                        <!--
                            MapStruct Processor

                            Génère automatiquement les implémentations
                            des interfaces annotées avec @Mapper.
                        -->
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>


                        <!--
                            Lombok Processor

                            Génère notamment :
                            - getters
                            - setters
                            - constructeurs
                            - etc.
                        -->
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>


                        <!--
                            Binding Lombok / MapStruct

                            Permet à MapStruct de correctement
                            prendre en compte les propriétés générées
                            par Lombok lors de la compilation.
                        -->
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok-mapstruct-binding.version}</version>
                        </path>

                    </annotationProcessorPaths>


                    <!-- =================================================
                         Options MapStruct
                         ================================================= -->

                    <compilerArgs>

                        <!--
                            Génère des mappers compatibles Spring.

                            Exemple :

                            @Mapper(componentModel = "spring")

                            peut être utilisé directement et le mapper
                            sera enregistré comme bean Spring.
                        -->
                        <arg>
                            -Amapstruct.defaultComponentModel=spring
                        </arg>


                        <!--
                            Important :

                            Nous voulons que MapStruct signale les champs
                            non correctement mappés.

                            Cela évite qu'un nouveau champ ajouté à une
                            entité ou à un DTO soit silencieusement oublié.
                        -->
                        <arg>
                            -Amapstruct.unmappedTargetPolicy=ERROR
                        </arg>

                    </compilerArgs>

                </configuration>

            </plugin>

        </plugins>

    </build>

</project>
